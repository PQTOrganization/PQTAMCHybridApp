import { ValidatorForm, TextValidator } from "react-material-ui-form-validator";

export const ValidatorFormDX = (props: any) => {
  return <ValidatorForm {...props} />;
};

export const TextValidatorDX = (props: any) => {
  return <TextValidator {...props} />;
};
