import { Divider } from "@mui/material";

const GridRowDividerDX = () => {
  return (
    <Divider sx={{ width: "100%", borderColor: "rgba(255, 0, 0, 0.08)" }} />
  );
};

export default GridRowDividerDX;
